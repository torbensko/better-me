import * as React from "react";

import { ToggleGroup } from ".";

export const DemoStandard = () => {
  return <>
    <ToggleGroup count={1} />
    <ToggleGroup count={2} />
    <ToggleGroup count={3} />
  </>;
};
